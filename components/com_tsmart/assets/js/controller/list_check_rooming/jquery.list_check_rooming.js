//huong dan su dung/* $('.list_check_rooming').list_check_rooming(); list_check_rooming=$('.list_check_rooming').data('list_check_rooming'); console.log(list_check_rooming); */// jQuery Plugin for SprFlat admin list_check_rooming// Control options and basic function of list_check_rooming// version 1.0, 28.02.2013// by SuggeElson www.suggeelson.com(function ($) {    // here we go!    $.list_check_rooming = function (element, options) {        // plugin's default options        var defaults = {            on_change:null            //main color scheme for list_check_rooming            //be sure to be same as colors on main.css or custom-variables.less        }        // current instance of the object        var plugin = this;        // this will hold the merged default, and user-provided options        plugin.settings = {}        var $element = $(element), // reference to the jQuery version of DOM element            element = element;    // reference to the actual DOM element        // the "constructor" method that gets called when the object is created        plugin.get_value=function(){            var element_name= plugin.settings.element_name;            var list_value_of_checked=[];            var $list_checked=$element.find('input[name="'+element_name+'"]:checked');            if($list_checked.length>0)            {                for(var i=0;i<$list_checked.length;i++){                    var $checked=$list_checked.eq(i);                    list_value_of_checked.push($checked.val());                }            }            return list_value_of_checked;        };        plugin.init = function () {            plugin.settings = $.extend({}, defaults, options);            plugin.settings.element_name_checked=plugin.settings.element_name;            plugin.settings.element_name=plugin.settings.element_name+'[]';            var element_name= plugin.settings.element_name;            $element.find('input[name="'+element_name+'"]').checkator({}).change(function(){                var on_change= plugin.settings.on_change;                var element_name_checked= plugin.settings.element_name_checked;                var selected=$element.find(':input:checked').serializeObject();                if(on_change instanceof Function){                    if(typeof selected[element_name_checked]=="undefined")                    {                        on_change([]);                    }else {                        on_change(selected[element_name_checked]);                    }                }            });        }        plugin.example_function = function () {        }        plugin.init();    }    // add the plugin to the jQuery.fn object    $.fn.list_check_rooming = function (options) {        // iterate through the DOM elements we are attaching the plugin to        return this.each(function () {            // if plugin has not already been attached to the element            if (undefined == $(this).data('list_check_rooming')) {                var plugin = new $.list_check_rooming(this, options);                $(this).data('list_check_rooming', plugin);            }        });    }})(jQuery);